package com.yi.interviewquestions.Facebook;

public class NestedListWeightSum {


/*    int result;
    public int depthSum(List<NestedInteger> nestedList) {
        result = 0;
        for(int i = 0; i< nestedList.size(); i++){

            depth(nestedList.get(i), 1);
        }
        return result;
    }

    public void depth(NestedInteger nestedList, int d){
        if(nestedList.isInteger()){
            result += nestedList.getInteger() * d;
            return;
        }

        for(int i = 0; i< nestedList.getList().size(); i++){
            depth(nestedList.getList().get(i),d+1);
        }

        return;
    }*/
}
